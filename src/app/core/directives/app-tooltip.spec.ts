import { AppTooltip } from './app-tooltip';

describe('AppTooltip', () => {
  it('should create an instance', () => {
    const directive = new AppTooltip();
    expect(directive).toBeTruthy();
  });
});
